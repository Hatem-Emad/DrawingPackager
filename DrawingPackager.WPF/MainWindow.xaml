<Window x:Class="DrawingPackager.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DrawingPackager.WPF"
        mc:Ignorable="d"
        Title="Drawing Packager" Height="260" Width="460"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="22" FontWeight="SemiBold" Text="Drawing Packager" />

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Width="140" Height="34" Click="ConnectButton_Click" Content="Connect" />
            <Button Width="140" Height="34" Margin="12,0,0,0" Click="StartButton_Click" Content="Start Solid Edge" />
        </StackPanel>

        <TextBlock x:Name="StatusText"
                   Grid.Row="4"
                   TextWrapping="Wrap"
                   VerticalAlignment="Top"
                   Text="Solid Edge is not connected." />
    </Grid>
</Window>
